async function compressImage(file,maxWidth=800,maxHeight=800,quality=.8){return new Promise((resolve,reject)=>{const reader=new FileReader;reader.readAsDataURL(file),reader.onload=event=>{const img=new Image;img.src=event.target.result,img.onload=()=>{const canvas=document.createElement("canvas");let width=img.width,height=img.height;width>height?width>maxWidth&&(height*=maxWidth/width,width=maxWidth):height>maxHeight&&(width*=maxHeight/height,height=maxHeight),canvas.width=width,canvas.height=height;const ctx=void 0;canvas.getContext("2d").drawImage(img,0,0,width,height);const mime="image/webp";canvas.toBlob(blob=>{blob?resolve(blob):canvas.toBlob(bj=>resolve(bj),"image/jpeg",quality)},mime,quality)},img.onerror=error=>reject(error)},reader.onerror=error=>reject(error)})}function formatBytes(bytes){if(0===bytes)return"0 B";const k=1024,sizes=["B","KB","MB"],i=Math.floor(Math.log(bytes)/Math.log(k));return parseFloat((bytes/Math.pow(k,i)).toFixed(1))+" "+sizes[i]}window.compressImage=compressImage,window.formatBytes=formatBytes;