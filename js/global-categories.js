async function fetchAppCategories(){if(window.appCategoriesList)return window.appCategoriesList;try{const response=await fetch(`/shared/list.json?v=${Date.now()}`);if(!response.ok)throw new Error("Failed to load list.json");return window.appCategoriesList=await response.json(),console.log("✅ [Global] تم تحميل قائمة الفئات بنجاح."),window.appCategoriesList}catch(error){return console.error("❌ [Global] خطأ في تحميل قائمة الفئات:",error),null}}window.appCategoriesList=null,window.normalizeCategorySelection=function(mainId,subId=null){return{mainId:null==mainId?"":String(mainId),subId:null==subId?"":String(subId)}},window.getCompatibleCategorySelections=function(mainId,subId=null){const normalized=void 0;return[window.normalizeCategorySelection(mainId,subId)]},window.normalizeBusinessCategoryMap=function(raw){if(!raw)return{};let parsed=raw;if("string"==typeof parsed){const trimmed=parsed.trim();if(!trimmed)return{};if(!trimmed.startsWith("{"))return{[String(trimmed)]:[]};try{parsed=JSON.parse(trimmed)}catch(error){return console.warn("[CategoryCompatibility] Failed to parse business category map:",error),{}}}if(!parsed||"object"!=typeof parsed||Array.isArray(parsed))return{};const normalized={};return Object.entries(parsed).forEach(([mainId,subIds])=>{const mainKey=String(mainId),subList=Array.isArray(subIds)?subIds.map(String):[];normalized[mainKey]=subList}),normalized},window.selectedSearchProductsSet=new Set;