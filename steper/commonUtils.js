import{ITEM_STATUS_ARABIC}from"./config.js";export function translateStatus(status){return"function"==typeof window.langu?window.langu(`status_${status}`)||status:ITEM_STATUS_ARABIC[status]||status}export function getProductName(productKey,ordersData){if(!ordersData||!Array.isArray(ordersData))return productKey;for(const order of ordersData)if(order.order_items&&Array.isArray(order.order_items)){const item=order.order_items.find(i=>i.product_key===productKey);if(item)return item.product_name}return productKey}