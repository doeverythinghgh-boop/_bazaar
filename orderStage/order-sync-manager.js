const OrderSyncManager={orderSync:async role=>{try{console.log(`[OrderSync] Starting sync for role: ${role}`);const lastTimestamp=await orderGetLastTimestamp(role);console.log(`[OrderSync] Last local order date: ${lastTimestamp||"No history"}`);const userKey=userSession.user_key;let apiRole="purchaser";"seller"===role&&(apiRole="seller"),"delivery"===role&&(apiRole="delivery"),"admin"===role&&(apiRole="admin");let url=`${baseURL}/api/user-all-orders?user_key=${userKey}&role=${apiRole}`;lastTimestamp&&(url+=`&after_date=${encodeURIComponent(lastTimestamp)}`);const response=await fetch(url);if(!response.ok)throw new Error(`HTTP ${response.status}`);const serverOrders=await response.json();console.log(`[OrderSync] Received ${serverOrders.length} orders from server.`);const savePromises=serverOrders.map(order=>orderSaveToLocalDB(order,role));return await Promise.all(savePromises),console.log("[OrderSync] Sync completed successfully."),serverOrders}catch(error){throw console.error("[OrderSync] Sync failed:",error),error}}};