const sharp=require("sharp"),path=require("path");async function getMostCommonColors(){const filePath=path.join(__dirname,"favicon.png");try{const{data:data,info:info}=await sharp(filePath).ensureAlpha().raw().toBuffer({resolveWithObject:!0}),{channels:channels}=info,counts={};for(let i=0;i<data.length;i+=channels){const r=data[i],g=data[i+1],b=data[i+2];if(0===data[i+3])continue;const key=`${r},${g},${b}`;counts[key]=(counts[key]||0)+1}const sorted=Object.entries(counts).sort((a,b)=>b[1]-a[1]);console.log("Top colors in favicon.png:"),console.log(JSON.stringify(sorted.slice(0,10)))}catch(err){console.error(err)}}getMostCommonColors();