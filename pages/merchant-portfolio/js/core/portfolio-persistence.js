window.portfolioPersistence=function(){const PREFIX="pp_",DEFAULT_TTL=864e5;function createKey(userKey,type,params="",offset=0){const cleanParams=void 0;return`pp_${userKey}_${type}_${String(params).replace(/[^a-z0-9]/gi,"_").toLowerCase()}_${offset}`}function save(userKey,type,params,offset,data){try{const key=createKey(userKey,type,params,offset),entry={timestamp:Date.now(),data:data};LocalDBStorage.setItem(key,JSON.stringify(entry)),console.log(`[Persistence] Saved: ${key}`)}catch(e){console.error("[Persistence] Save failed:",e)}}function get(userKey,type,params,offset){try{const key=createKey(userKey,type,params,offset),raw=LocalDBStorage.getItem(key);if(!raw)return null;const entry=JSON.parse(raw),age=Date.now()-entry.timestamp;return age>864e5?(console.log(`[Persistence] Expired: ${key} (Age: ${Math.round(age/36e5)}h)`),LocalDBStorage.removeItem(key),null):(console.log(`[Persistence] Cache Hit: ${key}`),entry.data)}catch(e){return console.error("[Persistence] Get failed:",e),null}}function clearMerchant(userKey){try{const searchPrefix=`pp_${userKey}_`,keysToRemove=[];for(let i=0;i<LocalDBStorage.length;i++){const key=LocalDBStorage.key(i);key&&key.startsWith(searchPrefix)&&keysToRemove.push(key)}keysToRemove.forEach(k=>LocalDBStorage.removeItem(k)),console.log(`[Persistence] Cleared all data for merchant: ${userKey}`)}catch(e){console.error("[Persistence] Clear failed:",e)}}function autoPurge(){try{const keysToRemove=[];for(let i=0;i<LocalDBStorage.length;i++){const key=LocalDBStorage.key(i);if(key&&key.startsWith("pp_")){const raw=LocalDBStorage.getItem(key);if(raw){const entry=JSON.parse(raw);Date.now()-entry.timestamp>864e5&&keysToRemove.push(key)}}}keysToRemove.forEach(k=>LocalDBStorage.removeItem(k)),keysToRemove.length>0&&console.log(`[Persistence] Auto-purged ${keysToRemove.length} expired entries.`)}catch(e){console.warn("[Persistence] Auto-purge failed:",e)}}return autoPurge(),{save:save,get:get,clearMerchant:clearMerchant,autoPurge:autoPurge}}();