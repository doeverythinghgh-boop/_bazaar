async function EDIT_openDesktopCamera(){const cameraModalContainer=document.getElementById("camera-modal-container");if(!cameraModalContainer)return console.error("Camera modal container not found!"),void 0;cameraModalContainer.innerHTML='\n        <div class="modal-content camera-modal-content">\n            <button class="close-button" id="camera-modal-close-btn" aria-label="Close"><i class="fas fa-times"></i></button>\n            <video id="camera-preview" autoplay playsinline></video>\n            <canvas id="camera-canvas" style="display:none;"></canvas>\n            <div class="camera-controls">\n                <button id="capture-photo-btn" class="btn btn-warning"><i class="fas fa-camera"></i> Capture Photo</button>\n            </div>\n        </div>\n    ',cameraModalContainer.style.display="flex";const video=document.getElementById("camera-preview"),captureBtn=document.getElementById("capture-photo-btn"),closeBtn=document.getElementById("camera-modal-close-btn");try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment"}});video.srcObject=stream;const closeStream=()=>{stream.getTracks().forEach(track=>track.stop()),cameraModalContainer.style.display="none",cameraModalContainer.innerHTML=""};closeBtn.onclick=closeStream,captureBtn.onclick=()=>{const canvas=document.getElementById("camera-canvas");canvas.width=video.videoWidth,canvas.height=video.videoHeight,canvas.getContext("2d").drawImage(video,0,0),canvas.toBlob(blob=>{EDIT_handleNewFiles([blob]),closeStream()},"image/jpeg",.9)}}catch(err){console.error("Error accessing camera: ",err),cameraModalContainer.style.display="none"}}