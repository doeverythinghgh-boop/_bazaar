function adminResolveText(key,fallback){const value="function"==typeof window.langu?window.langu(key):null;return value&&value!==key?value:fallback}function adminApiText(key,fallback){return adminResolveText(key,fallback)}function adminUiText(key,fallback){return adminResolveText(key,fallback)}function adminSearchText(key,fallback){return adminResolveText(key,fallback)}function adminAdsText(key,fallback){return adminResolveText(key,fallback)}function adminActionText(key,fallback){return adminResolveText(key,fallback)}function adminInitText(key,fallback){return adminResolveText(key,fallback)}function adminRelationsText(key,fallback){return adminResolveText(key,fallback)}function adminPendingText(key,fallback){return adminResolveText(key,fallback)}function adminUnwrapPayload(payload){if(payload&&"object"==typeof payload&&Object.prototype.hasOwnProperty.call(payload,"success")&&Object.prototype.hasOwnProperty.call(payload,"data")&&Object.prototype.hasOwnProperty.call(payload,"error")){if(!1===payload.success)throw new Error(payload?.error?.message||"API request failed");return payload.data}return payload}