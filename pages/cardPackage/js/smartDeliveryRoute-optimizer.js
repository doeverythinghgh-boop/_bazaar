function findShortestDeliveryRoute(office,customer,sellers){if(!sellers.length)return{distance:calculateDistance(office,customer),route:[]};const permutations=generatePermutations(sellers);let shortestDistance=1/0,bestRoute=[];for(const path of permutations){const distance=calculateRouteDistance(office,path,customer);distance<shortestDistance&&(shortestDistance=distance,bestRoute=path)}return{distance:shortestDistance,route:bestRoute}}