function findShortestDeliveryRoute(office,customer,merchants){if(!merchants.length)return{distance:calculateDistance(office,customer),route:[]};const permutations=generatePermutations(merchants);let shortestDistance=1/0,bestRoute=[];for(const path of permutations){const distance=calculateRouteDistance(office,path,customer);distance<shortestDistance&&(shortestDistance=distance,bestRoute=path)}return{distance:shortestDistance,route:bestRoute}}