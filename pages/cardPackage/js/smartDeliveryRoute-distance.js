function calculateDistance(pointA,pointB){const latDiff=pointB.lat-pointA.lat,lngDiff=pointB.lng-pointA.lng;return Math.sqrt(latDiff**2+lngDiff**2)}function calculateRouteDistance(office,sellersPath,customer){let totalDistance=0;totalDistance+=calculateDistance(office,sellersPath[0]);for(let i=0;i<sellersPath.length-1;i++)totalDistance+=calculateDistance(sellersPath[i],sellersPath[i+1]);return totalDistance+=calculateDistance(sellersPath[sellersPath.length-1],customer),totalDistance}