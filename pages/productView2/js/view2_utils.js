const PV2_WEBP_SUPPORTED_PROMISE=(async()=>{const canvas=document.createElement("canvas");return!(!canvas.getContext||!canvas.getContext("2d"))&&0===canvas.toDataURL("image/webp").indexOf("data:image/webp")})();async function pv2_compressImage(file){let imgBitmap;try{imgBitmap=await createImageBitmap(file)}catch(e){return console.warn("فشل ضغط الصورة، العودة للنسخة الأصلية",e),file}const width=imgBitmap.width,height=imgBitmap.height,ratio=Math.min(1,PV2_IMAGE_MAX_WIDTH/width,PV2_IMAGE_MAX_HEIGHT/height),newWidth=Math.round(width*ratio),newHeight=Math.round(height*ratio),canvas=Object.assign(document.createElement("canvas"),{width:newWidth,height:newHeight}),ctx=canvas.getContext("2d");ctx.fillStyle="#ffffff",ctx.fillRect(0,0,newWidth,newHeight),ctx.drawImage(imgBitmap,0,0,newWidth,newHeight);const webpSupported=await PV2_WEBP_SUPPORTED_PROMISE,mime=webpSupported?"image/webp":"image/jpeg",blob=await new Promise(res=>canvas.toBlob(res,mime,PV2_IMAGE_QUALITY));try{imgBitmap.close()}catch(e){}return blob.name=file.name,blob.lastModified=file.lastModified,blob.isCompressed=!0,blob.extension=webpSupported?"webp":"jpg",blob}