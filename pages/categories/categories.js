/**
 * @file pages/categories/categories.js
 * @description Master script for categories module.
 * This file serves as a consolidated entry point that loads all modular sub-scripts.
 * Features: Core navigation, Category Loading, UI Grid Toggling, and Product Gallery.
 */
/**
 * DEVELOPER NOTICE:
 * All terminal/console messages must be in pure English without emojis or translation keys.
 * Technical errors (exceptions) should only be logged to the console and not displayed via Swal.
 * Every developer must ensure that the terminal reflects code execution step by step,
 * logging each significant operation in sequence so the execution flow is fully traceable.
 */


// Note: In modular environments, these would be imports.
// In this project, they are loaded as separate script tags in the HTML file
// to ensure global availability of all categories_* functions.

console.log(" [Categories] Module Loaded.");
